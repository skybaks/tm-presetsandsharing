
namespace Interface
{
    VirtualKey ComboboxVirtualKey()
    {
        if (UI::Selectable(tostring(VirtualKey::LButton), false))
        {
            return VirtualKey::LButton;
        }
        if (UI::Selectable(tostring(VirtualKey::RButton), false))
        {
            return VirtualKey::RButton;
        }
        if (UI::Selectable(tostring(VirtualKey::Cancel), false))
        {
            return VirtualKey::Cancel;
        }
        if (UI::Selectable(tostring(VirtualKey::Mbutton), false))
        {
            return VirtualKey::Mbutton;
        }
        if (UI::Selectable(tostring(VirtualKey::XButton1), false))
        {
            return VirtualKey::XButton1;
        }
        if (UI::Selectable(tostring(VirtualKey::XButton2), false))
        {
            return VirtualKey::XButton2;
        }
        if (UI::Selectable(tostring(VirtualKey::Back), false))
        {
            return VirtualKey::Back;
        }
        if (UI::Selectable(tostring(VirtualKey::Tab), false))
        {
            return VirtualKey::Tab;
        }
        if (UI::Selectable(tostring(VirtualKey::Clear), false))
        {
            return VirtualKey::Clear;
        }
        if (UI::Selectable(tostring(VirtualKey::Return), false))
        {
            return VirtualKey::Return;
        }
        if (UI::Selectable(tostring(VirtualKey::Shift), false))
        {
            return VirtualKey::Shift;
        }
        if (UI::Selectable(tostring(VirtualKey::Control), false))
        {
            return VirtualKey::Control;
        }
        if (UI::Selectable(tostring(VirtualKey::Menu), false))
        {
            return VirtualKey::Menu;
        }
        if (UI::Selectable(tostring(VirtualKey::Pause), false))
        {
            return VirtualKey::Pause;
        }
        if (UI::Selectable(tostring(VirtualKey::Capital), false))
        {
            return VirtualKey::Capital;
        }
        if (UI::Selectable(tostring(VirtualKey::Kana), false))
        {
            return VirtualKey::Kana;
        }
        if (UI::Selectable(tostring(VirtualKey::Hanguel), false))
        {
            return VirtualKey::Hanguel;
        }
        if (UI::Selectable(tostring(VirtualKey::Hangul), false))
        {
            return VirtualKey::Hangul;
        }
        if (UI::Selectable(tostring(VirtualKey::Junja), false))
        {
            return VirtualKey::Junja;
        }
        if (UI::Selectable(tostring(VirtualKey::Final), false))
        {
            return VirtualKey::Final;
        }
        if (UI::Selectable(tostring(VirtualKey::Hanja), false))
        {
            return VirtualKey::Hanja;
        }
        if (UI::Selectable(tostring(VirtualKey::Kanji), false))
        {
            return VirtualKey::Kanji;
        }
        if (UI::Selectable(tostring(VirtualKey::Escape), false))
        {
            return VirtualKey::Escape;
        }
        if (UI::Selectable(tostring(VirtualKey::Convert), false))
        {
            return VirtualKey::Convert;
        }
        if (UI::Selectable(tostring(VirtualKey::NonConvert), false))
        {
            return VirtualKey::NonConvert;
        }
        if (UI::Selectable(tostring(VirtualKey::Accept), false))
        {
            return VirtualKey::Accept;
        }
        if (UI::Selectable(tostring(VirtualKey::ModeChange), false))
        {
            return VirtualKey::ModeChange;
        }
        if (UI::Selectable(tostring(VirtualKey::Space), false))
        {
            return VirtualKey::Space;
        }
        if (UI::Selectable(tostring(VirtualKey::Prior), false))
        {
            return VirtualKey::Prior;
        }
        if (UI::Selectable(tostring(VirtualKey::Next), false))
        {
            return VirtualKey::Next;
        }
        if (UI::Selectable(tostring(VirtualKey::End), false))
        {
            return VirtualKey::End;
        }
        if (UI::Selectable(tostring(VirtualKey::Home), false))
        {
            return VirtualKey::Home;
        }
        if (UI::Selectable(tostring(VirtualKey::Left), false))
        {
            return VirtualKey::Left;
        }
        if (UI::Selectable(tostring(VirtualKey::Up), false))
        {
            return VirtualKey::Up;
        }
        if (UI::Selectable(tostring(VirtualKey::Right), false))
        {
            return VirtualKey::Right;
        }
        if (UI::Selectable(tostring(VirtualKey::Down), false))
        {
            return VirtualKey::Down;
        }
        if (UI::Selectable(tostring(VirtualKey::Select), false))
        {
            return VirtualKey::Select;
        }
        if (UI::Selectable(tostring(VirtualKey::Print), false))
        {
            return VirtualKey::Print;
        }
        if (UI::Selectable(tostring(VirtualKey::Execute), false))
        {
            return VirtualKey::Execute;
        }
        if (UI::Selectable(tostring(VirtualKey::Snapshot), false))
        {
            return VirtualKey::Snapshot;
        }
        if (UI::Selectable(tostring(VirtualKey::Insert), false))
        {
            return VirtualKey::Insert;
        }
        if (UI::Selectable(tostring(VirtualKey::Delete), false))
        {
            return VirtualKey::Delete;
        }
        if (UI::Selectable(tostring(VirtualKey::Help), false))
        {
            return VirtualKey::Help;
        }
        if (UI::Selectable(tostring(VirtualKey::N0), false))
        {
            return VirtualKey::N0;
        }
        if (UI::Selectable(tostring(VirtualKey::N1), false))
        {
            return VirtualKey::N1;
        }
        if (UI::Selectable(tostring(VirtualKey::N2), false))
        {
            return VirtualKey::N2;
        }
        if (UI::Selectable(tostring(VirtualKey::N3), false))
        {
            return VirtualKey::N3;
        }
        if (UI::Selectable(tostring(VirtualKey::N4), false))
        {
            return VirtualKey::N4;
        }
        if (UI::Selectable(tostring(VirtualKey::N5), false))
        {
            return VirtualKey::N5;
        }
        if (UI::Selectable(tostring(VirtualKey::N6), false))
        {
            return VirtualKey::N6;
        }
        if (UI::Selectable(tostring(VirtualKey::N7), false))
        {
            return VirtualKey::N7;
        }
        if (UI::Selectable(tostring(VirtualKey::N8), false))
        {
            return VirtualKey::N8;
        }
        if (UI::Selectable(tostring(VirtualKey::N9), false))
        {
            return VirtualKey::N9;
        }
        if (UI::Selectable(tostring(VirtualKey::A), false))
        {
            return VirtualKey::A;
        }
        if (UI::Selectable(tostring(VirtualKey::B), false))
        {
            return VirtualKey::B;
        }
        if (UI::Selectable(tostring(VirtualKey::C), false))
        {
            return VirtualKey::C;
        }
        if (UI::Selectable(tostring(VirtualKey::D), false))
        {
            return VirtualKey::D;
        }
        if (UI::Selectable(tostring(VirtualKey::E), false))
        {
            return VirtualKey::E;
        }
        if (UI::Selectable(tostring(VirtualKey::F), false))
        {
            return VirtualKey::F;
        }
        if (UI::Selectable(tostring(VirtualKey::G), false))
        {
            return VirtualKey::G;
        }
        if (UI::Selectable(tostring(VirtualKey::H), false))
        {
            return VirtualKey::H;
        }
        if (UI::Selectable(tostring(VirtualKey::I), false))
        {
            return VirtualKey::I;
        }
        if (UI::Selectable(tostring(VirtualKey::J), false))
        {
            return VirtualKey::J;
        }
        if (UI::Selectable(tostring(VirtualKey::K), false))
        {
            return VirtualKey::K;
        }
        if (UI::Selectable(tostring(VirtualKey::L), false))
        {
            return VirtualKey::L;
        }
        if (UI::Selectable(tostring(VirtualKey::M), false))
        {
            return VirtualKey::M;
        }
        if (UI::Selectable(tostring(VirtualKey::N), false))
        {
            return VirtualKey::N;
        }
        if (UI::Selectable(tostring(VirtualKey::O), false))
        {
            return VirtualKey::O;
        }
        if (UI::Selectable(tostring(VirtualKey::P), false))
        {
            return VirtualKey::P;
        }
        if (UI::Selectable(tostring(VirtualKey::Q), false))
        {
            return VirtualKey::Q;
        }
        if (UI::Selectable(tostring(VirtualKey::R), false))
        {
            return VirtualKey::R;
        }
        if (UI::Selectable(tostring(VirtualKey::S), false))
        {
            return VirtualKey::S;
        }
        if (UI::Selectable(tostring(VirtualKey::T), false))
        {
            return VirtualKey::T;
        }
        if (UI::Selectable(tostring(VirtualKey::U), false))
        {
            return VirtualKey::U;
        }
        if (UI::Selectable(tostring(VirtualKey::V), false))
        {
            return VirtualKey::V;
        }
        if (UI::Selectable(tostring(VirtualKey::W), false))
        {
            return VirtualKey::W;
        }
        if (UI::Selectable(tostring(VirtualKey::X), false))
        {
            return VirtualKey::X;
        }
        if (UI::Selectable(tostring(VirtualKey::Y), false))
        {
            return VirtualKey::Y;
        }
        if (UI::Selectable(tostring(VirtualKey::Z), false))
        {
            return VirtualKey::Z;
        }
        if (UI::Selectable(tostring(VirtualKey::Lwin), false))
        {
            return VirtualKey::Lwin;
        }
        if (UI::Selectable(tostring(VirtualKey::Rwin), false))
        {
            return VirtualKey::Rwin;
        }
        if (UI::Selectable(tostring(VirtualKey::Apps), false))
        {
            return VirtualKey::Apps;
        }
        if (UI::Selectable(tostring(VirtualKey::Sleep), false))
        {
            return VirtualKey::Sleep;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad0), false))
        {
            return VirtualKey::Numpad0;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad1), false))
        {
            return VirtualKey::Numpad1;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad2), false))
        {
            return VirtualKey::Numpad2;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad3), false))
        {
            return VirtualKey::Numpad3;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad4), false))
        {
            return VirtualKey::Numpad4;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad5), false))
        {
            return VirtualKey::Numpad5;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad6), false))
        {
            return VirtualKey::Numpad6;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad7), false))
        {
            return VirtualKey::Numpad7;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad8), false))
        {
            return VirtualKey::Numpad8;
        }
        if (UI::Selectable(tostring(VirtualKey::Numpad9), false))
        {
            return VirtualKey::Numpad9;
        }
        if (UI::Selectable(tostring(VirtualKey::Multiply), false))
        {
            return VirtualKey::Multiply;
        }
        if (UI::Selectable(tostring(VirtualKey::Add), false))
        {
            return VirtualKey::Add;
        }
        if (UI::Selectable(tostring(VirtualKey::Separator), false))
        {
            return VirtualKey::Separator;
        }
        if (UI::Selectable(tostring(VirtualKey::Subtract), false))
        {
            return VirtualKey::Subtract;
        }
        if (UI::Selectable(tostring(VirtualKey::Decimal), false))
        {
            return VirtualKey::Decimal;
        }
        if (UI::Selectable(tostring(VirtualKey::Divide), false))
        {
            return VirtualKey::Divide;
        }
        if (UI::Selectable(tostring(VirtualKey::F1), false))
        {
            return VirtualKey::F1;
        }
        if (UI::Selectable(tostring(VirtualKey::F2), false))
        {
            return VirtualKey::F2;
        }
        if (UI::Selectable(tostring(VirtualKey::F3), false))
        {
            return VirtualKey::F3;
        }
        if (UI::Selectable(tostring(VirtualKey::F4), false))
        {
            return VirtualKey::F4;
        }
        if (UI::Selectable(tostring(VirtualKey::F5), false))
        {
            return VirtualKey::F5;
        }
        if (UI::Selectable(tostring(VirtualKey::F6), false))
        {
            return VirtualKey::F6;
        }
        if (UI::Selectable(tostring(VirtualKey::F7), false))
        {
            return VirtualKey::F7;
        }
        if (UI::Selectable(tostring(VirtualKey::F8), false))
        {
            return VirtualKey::F8;
        }
        if (UI::Selectable(tostring(VirtualKey::F9), false))
        {
            return VirtualKey::F9;
        }
        if (UI::Selectable(tostring(VirtualKey::F10), false))
        {
            return VirtualKey::F10;
        }
        if (UI::Selectable(tostring(VirtualKey::F11), false))
        {
            return VirtualKey::F11;
        }
        if (UI::Selectable(tostring(VirtualKey::F12), false))
        {
            return VirtualKey::F12;
        }
        if (UI::Selectable(tostring(VirtualKey::F13), false))
        {
            return VirtualKey::F13;
        }
        if (UI::Selectable(tostring(VirtualKey::F14), false))
        {
            return VirtualKey::F14;
        }
        if (UI::Selectable(tostring(VirtualKey::F15), false))
        {
            return VirtualKey::F15;
        }
        if (UI::Selectable(tostring(VirtualKey::F16), false))
        {
            return VirtualKey::F16;
        }
        if (UI::Selectable(tostring(VirtualKey::F17), false))
        {
            return VirtualKey::F17;
        }
        if (UI::Selectable(tostring(VirtualKey::F18), false))
        {
            return VirtualKey::F18;
        }
        if (UI::Selectable(tostring(VirtualKey::F19), false))
        {
            return VirtualKey::F19;
        }
        if (UI::Selectable(tostring(VirtualKey::F20), false))
        {
            return VirtualKey::F20;
        }
        if (UI::Selectable(tostring(VirtualKey::F21), false))
        {
            return VirtualKey::F21;
        }
        if (UI::Selectable(tostring(VirtualKey::F22), false))
        {
            return VirtualKey::F22;
        }
        if (UI::Selectable(tostring(VirtualKey::F23), false))
        {
            return VirtualKey::F23;
        }
        if (UI::Selectable(tostring(VirtualKey::F24), false))
        {
            return VirtualKey::F24;
        }
        if (UI::Selectable(tostring(VirtualKey::NumLock), false))
        {
            return VirtualKey::NumLock;
        }
        if (UI::Selectable(tostring(VirtualKey::Scroll), false))
        {
            return VirtualKey::Scroll;
        }
        if (UI::Selectable(tostring(VirtualKey::LShift), false))
        {
            return VirtualKey::LShift;
        }
        if (UI::Selectable(tostring(VirtualKey::RShift), false))
        {
            return VirtualKey::RShift;
        }
        if (UI::Selectable(tostring(VirtualKey::LControl), false))
        {
            return VirtualKey::LControl;
        }
        if (UI::Selectable(tostring(VirtualKey::RControl), false))
        {
            return VirtualKey::RControl;
        }
        if (UI::Selectable(tostring(VirtualKey::LMenu), false))
        {
            return VirtualKey::LMenu;
        }
        if (UI::Selectable(tostring(VirtualKey::RMenu), false))
        {
            return VirtualKey::RMenu;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserBack), false))
        {
            return VirtualKey::BrowserBack;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserForward), false))
        {
            return VirtualKey::BrowserForward;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserRefresh), false))
        {
            return VirtualKey::BrowserRefresh;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserStop), false))
        {
            return VirtualKey::BrowserStop;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserSearch), false))
        {
            return VirtualKey::BrowserSearch;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserFavorites), false))
        {
            return VirtualKey::BrowserFavorites;
        }
        if (UI::Selectable(tostring(VirtualKey::BrowserHome), false))
        {
            return VirtualKey::BrowserHome;
        }
        if (UI::Selectable(tostring(VirtualKey::VolumeMute), false))
        {
            return VirtualKey::VolumeMute;
        }
        if (UI::Selectable(tostring(VirtualKey::VolumeDown), false))
        {
            return VirtualKey::VolumeDown;
        }
        if (UI::Selectable(tostring(VirtualKey::VolumeUp), false))
        {
            return VirtualKey::VolumeUp;
        }
        if (UI::Selectable(tostring(VirtualKey::MediaNextTrack), false))
        {
            return VirtualKey::MediaNextTrack;
        }
        if (UI::Selectable(tostring(VirtualKey::MediaPrevTrack), false))
        {
            return VirtualKey::MediaPrevTrack;
        }
        if (UI::Selectable(tostring(VirtualKey::MediaStop), false))
        {
            return VirtualKey::MediaStop;
        }
        if (UI::Selectable(tostring(VirtualKey::MediaPlayPause), false))
        {
            return VirtualKey::MediaPlayPause;
        }
        if (UI::Selectable(tostring(VirtualKey::LaunchMail), false))
        {
            return VirtualKey::LaunchMail;
        }
        if (UI::Selectable(tostring(VirtualKey::LaunchMediaSelect), false))
        {
            return VirtualKey::LaunchMediaSelect;
        }
        if (UI::Selectable(tostring(VirtualKey::LaunchApp1), false))
        {
            return VirtualKey::LaunchApp1;
        }
        if (UI::Selectable(tostring(VirtualKey::LaunchApp2), false))
        {
            return VirtualKey::LaunchApp2;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem1), false))
        {
            return VirtualKey::Oem1;
        }
        if (UI::Selectable(tostring(VirtualKey::OemPlus), false))
        {
            return VirtualKey::OemPlus;
        }
        if (UI::Selectable(tostring(VirtualKey::OemComma), false))
        {
            return VirtualKey::OemComma;
        }
        if (UI::Selectable(tostring(VirtualKey::OemMinus), false))
        {
            return VirtualKey::OemMinus;
        }
        if (UI::Selectable(tostring(VirtualKey::OemPeriod), false))
        {
            return VirtualKey::OemPeriod;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem2), false))
        {
            return VirtualKey::Oem2;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem3), false))
        {
            return VirtualKey::Oem3;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem4), false))
        {
            return VirtualKey::Oem4;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem5), false))
        {
            return VirtualKey::Oem5;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem6), false))
        {
            return VirtualKey::Oem6;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem7), false))
        {
            return VirtualKey::Oem7;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem8), false))
        {
            return VirtualKey::Oem8;
        }
        if (UI::Selectable(tostring(VirtualKey::Oem102), false))
        {
            return VirtualKey::Oem102;
        }
        if (UI::Selectable(tostring(VirtualKey::ProcessKey), false))
        {
            return VirtualKey::ProcessKey;
        }
        if (UI::Selectable(tostring(VirtualKey::Packet), false))
        {
            return VirtualKey::Packet;
        }
        if (UI::Selectable(tostring(VirtualKey::Attn), false))
        {
            return VirtualKey::Attn;
        }
        if (UI::Selectable(tostring(VirtualKey::Crsel), false))
        {
            return VirtualKey::Crsel;
        }
        if (UI::Selectable(tostring(VirtualKey::Exsel), false))
        {
            return VirtualKey::Exsel;
        }
        if (UI::Selectable(tostring(VirtualKey::Ereof), false))
        {
            return VirtualKey::Ereof;
        }
        if (UI::Selectable(tostring(VirtualKey::Play), false))
        {
            return VirtualKey::Play;
        }
        if (UI::Selectable(tostring(VirtualKey::Zoom), false))
        {
            return VirtualKey::Zoom;
        }
        if (UI::Selectable(tostring(VirtualKey::Noname), false))
        {
            return VirtualKey::Noname;
        }
        if (UI::Selectable(tostring(VirtualKey::Pa1), false))
        {
            return VirtualKey::Pa1;
        }
        if (UI::Selectable(tostring(VirtualKey::OemClear), false))
        {
            return VirtualKey::OemClear;
        }
        return VirtualKey(0);
    }
}
